<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ── Card border ── -->
    <Style x:Key="Style.Card" TargetType="Border">
        <Setter Property="Background"   Value="{StaticResource Brush.Background.Card}"/>
        <Setter Property="CornerRadius" Value="{StaticResource Spacing.Card.CornerRadius}"/>
        <Setter Property="Padding"      Value="{StaticResource Spacing.Card.Padding}"/>
    </Style>

    <!-- ── Muted section label (e.g. "Used Space") ── -->
    <Style x:Key="Style.Label.Muted" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Muted}"/>
        <Setter Property="FontSize"   Value="{StaticResource Font.Size.Small}"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Family.Default}"/>
        <Setter Property="Margin"     Value="{StaticResource Spacing.MutedLabel.Margin}"/>
    </Style>

    <!-- ── Large metric value (e.g. "128.4 GB") ── -->
    <Style x:Key="Style.Value.Metric" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource Brush.Text.Primary}"/>
        <Setter Property="FontSize"   Value="{StaticResource Font.Size.Metric}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontFamily" Value="{StaticResource Font.Family.Default}"/>
    </Style>

    <!-- ── Detail row label (left column, e.g. "Health") ── -->
    <Style x:Key="Style.Label.Detail" TargetType="TextBlock">
        <Setter Property="Foreground"        Value="{StaticResource Brush.Text.Muted}"/>
        <Setter Property="FontSize"          Value="{StaticResource Font.Size.Small}"/>
        <Setter Property="FontFamily"        Value="{StaticResource Font.Family.Default}"/>
        <Setter Property="Margin"            Value="{StaticResource Spacing.Detail.Label.Margin}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- ── Detail row value (right column, e.g. "OK") ── -->
    <Style x:Key="Style.Value.Detail" TargetType="TextBlock">
        <Setter Property="Foreground"        Value="{StaticResource Brush.Text.Secondary}"/>
        <Setter Property="FontSize"          Value="{StaticResource Font.Size.Body}"/>
        <Setter Property="FontFamily"        Value="{StaticResource Font.Family.Default}"/>
        <Setter Property="Margin"            Value="{StaticResource Spacing.Detail.Value.Margin}"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>
